import * as postResolvers from './Post.js';
import * as profileResolver from './Profile.js';
import * as userResolver from './User.js';

export const Post = postResolvers;
export const Profile = profileResolver;
export const User = userResolver;
